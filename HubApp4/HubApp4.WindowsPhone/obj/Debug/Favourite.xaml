<Page
    x:Class="HubApp4.Favourite"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HubApp4"
    
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="WhiteSmoke">
    <!--xmlns:data="using:HubApp4.FavData"
    DataContext="{Binding DefaultViewModel.Item, RelativeSource={RelativeSource Self}}"
    d:DataContext="{Binding Groups, Source={d:DesignData Source=../HubApp4.Shared/DataModel/Fav.json, Type=data:SampleFavSource}}"-->

    <Page.Resources>
        <SolidColorBrush x:Name="foreg" Color="Black"></SolidColorBrush>
        <SolidColorBrush x:Name="foreg2" Color="Gray"></SolidColorBrush>
        <DataTemplate x:Key="ListItemTemplatex">
            <StackPanel  Orientation="Horizontal" Height="Auto" Width="350" >
                <StackPanel Height="Auto" Margin="5,0,5,15" Width="290">
                    <TextBlock TextWrapping="Wrap" Text="{Binding Title}" FontWeight="SemiBold" Style="{ThemeResource ListViewItemTextBlockStyle}" Foreground="{StaticResource foreg}"/>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,0">
                        <TextBlock  Foreground="{StaticResource foreg2}" Margin="0,0,5,0" TextWrapping="Wrap" Text="Time:  " Height="18" Style="{ThemeResource ListViewItemSubheaderTextBlockStyle}"/>
                        <TextBlock TextWrapping="Wrap" Text="{Binding Subtitle}" Foreground="{StaticResource foreg}" Height="18" Style="{ThemeResource ListViewItemSubheaderTextBlockStyle}"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,0">
                        <TextBlock Foreground="{StaticResource foreg2}" Margin="0,0,5,0" TextWrapping="Wrap" Text="Venue:  " Height="18" Style="{ThemeResource ListViewItemSubheaderTextBlockStyle}"/>
                        <TextBlock TextWrapping="Wrap" Text="{Binding Content}" Height="18" Style="{ThemeResource ListViewItemSubheaderTextBlockStyle}" Foreground="{StaticResource foreg}" />
                    </StackPanel>
                </StackPanel>
                <Button x:ConnectionId='1'  Margin="0,-15,0,0"  BorderBrush="{x:Null}"                      Tag="{Binding UniqueId}" MinWidth="50" >

                    <Image Source="Assets/mappin.png" Height="30"  Width="30"/>
                </Button>
            </StackPanel>
        </DataTemplate>
    </Page.Resources>

    <Grid x:Name="LayoutRoot">

        <Grid.ChildrenTransitions>
            <TransitionCollection>
                <EntranceThemeTransition/>
            </TransitionCollection>
        </Grid.ChildrenTransitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Title Panel -->
        <StackPanel Grid.Row="0" Margin="19,0,0,0">
            <TextBlock  Text="MY APPLICATION" Style="{ThemeResource TitleTextBlockStyle}" Foreground="Black" Margin="0,12,0,0"/>
            <TextBlock Text="Favourites" Margin="0,-6.5,0,26.5" Style="{ThemeResource HeaderTextBlockStyle}" Foreground="Gray" CharacterSpacing="{ThemeResource PivotHeaderItemCharacterSpacing}"/>
        </StackPanel>

        <!--TODO: Content should be placed within the following grid-->

        <Grid Grid.Row="1" x:Name="ContentRoot" Margin="19,9.5,19,0" >

            <ScrollViewer>
                <ListView x:ConnectionId='2'
                       x:Name="FavListView"
                        Margin="0,0,0,0"
                        AutomationProperties.AutomationId="ItemGridView"
                        AutomationProperties.Name="Items In Group"
                        ItemTemplate="{StaticResource ListItemTemplatex}"
                       IsItemClickEnabled="True"
                        SelectionMode="None"
                                                      
                       
                    
                                               
                        ContinuumNavigationTransitionInfo.ExitElementContainer="True"
                       >
                    <!-- ItemsSource="{Binding Groups}"
                        Tag="{Binding Id}"
                        ItemClick="ItemView_ItemClick"-->

                    <!--<GridView.ItemsPanel>
                            <ItemsPanelTemplate>
                                <ItemsWrapGrid />
                            </ItemsPanelTemplate>
                        </GridView.ItemsPanel>-->
                </ListView>


            </ScrollViewer>

        </Grid>
    </Grid>
</Page>

